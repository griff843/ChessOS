{
  "name": "worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx src/index.ts",
    "train-model": "tsx src/train.ts",
    "score-critical": "tsx src/score-critical.ts",
    "run-ablation": "tsx src/run-ablation.ts",
    "generate-targets": "tsx src/generate-training-targets.ts",
    "generate-exercises": "tsx src/generate-exercises.ts",
    "generate-session": "tsx src/generate-session.ts",
    "record-session": "tsx src/record-session.ts",
    "solve-session": "tsx src/solve-session.ts",
    "generate-dashboard": "tsx src/generate-dashboard.ts",
    "generate-coach-report": "tsx src/generate-coach-report.ts",
    "generate-curriculum": "tsx src/generate-curriculum.ts",
    "generate-patterns": "tsx src/generate-patterns.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo worker lint pending"
  },
  "dependencies": {
    "@chess-os/chess-core": "workspace:*",
    "@chess-os/classifier": "workspace:*",
    "@chess-os/db": "workspace:*",
    "@chess-os/engine": "workspace:*",
    "@chess-os/training": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
